server:
  port: 8081

spring:
  application:
    name: worker

orchestration:
  grpc:
    server:
      port: 9090

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
